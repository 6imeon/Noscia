// Corpus & index view (Phase 1) — Phase 0 placeholder shell.
export function Corpus() {
  return (
    <div className="p-6">
      <h1 className="mb-1 text-lg text-ink">Corpus &amp; index</h1>
      <p className="text-sm text-muted">
        Stat cards, the sources table with live crawl progress, and the add-seed input land in
        Phase 1 (SPEC §8.4).
      </p>
    </div>
  )
}
