// Entity search view (Phase 3) — Phase 0 placeholder shell.
export function EntitySearch() {
  return (
    <div className="p-6">
      <h1 className="mb-1 text-lg text-ink">Entity search</h1>
      <p className="text-sm text-muted">
        The agentic entity-extraction table (evidence-or-null, CSV/JSON export) lands in Phase 3
        (SPEC §8.4).
      </p>
    </div>
  )
}
